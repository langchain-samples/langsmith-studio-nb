# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# API keys and anything else that looks like a credential. Unanchored on purpose:
# this catches them at any depth, not just where the example keeps them.
.env
.env.*
!.env.example
*.key
*.pem
credentials.json
secrets.json

.coverage
.pytest_cache/
.ruff_cache/
.langgraph_api/
