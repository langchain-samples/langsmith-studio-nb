# Copy to .env in this directory and fill in. Git ignores .env; never commit real keys.
# The notebook beside this file loads it through python-dotenv's load_dotenv().

# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# https://smith.langchain.com/settings/apikeys
LANGSMITH_API_KEY=lsv2_pt_...
